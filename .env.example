# HuggingFace token (gratuit) — pour la génération d'images via FLUX.1-schnell
# 1. Créer un compte sur https://huggingface.co
# 2. Aller dans Settings > Access Tokens
# 3. Créer un token (type: Read)
HF_TOKEN=hf_xxx

# Auth Vercel AI Gateway via OIDC (optionnel)
# Lancer: vercel link && vercel env pull .env.local
